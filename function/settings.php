<?php

    // Settings for Dotcloud Server

    // $envjson = json_decode(file_get_contents("/home/dotcloud/environment.json"),true);
    // $host =  $envjson['DOTCLOUD_DB_MYSQL_HOST'];
    // $username = 'lockscreen';
    // $password = 'jXTHNyYU6R8W';
    // $db = 'lockscreen_db';
    // $port = $envjson['DOTCLOUD_DB_MYSQL_PORT'];

    // Settings for Local Test Server

    $host =  '127.0.0.1';
    $username = 'root';
    $password = 'password';
    $db = 'lockscreen_db';
    $port = 3306;
?>